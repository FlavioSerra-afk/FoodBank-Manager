# Shortcodes Reference

These examples cover common usages of FoodBank Manager shortcodes.

## Application Form
```
[fbm_form id="123" preset="basic_intake" mask_sensitive="true"]
```
- `id` – ID of the form to render.
- `preset` – Preset defining the form layout.
- `mask_sensitive` – Keep sensitive fields masked on output.

## Dashboard
```
[fbm_dashboard compare="true" range="last_30" preset="manager"]
```
- `compare` – Enable comparison charts.
- `range` – Time range such as `last_30`.
- `preset` – Dashboard preset.

See the Shortcodes page in the admin area for a generator and additional options.
